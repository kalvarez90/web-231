const header = require('./alvarez-header.js');

console.log(header.display("Karina", "Alvarez", "Exercise 4.1 - Arrays"));
console.log ('\n');
/*=======================================
; Title: Discussion Board 4.1
; Author: Professor Krasso
; Date: 16 June 2020
; Modify by: Karina Alvarez
; Description: Javascript program using
; arrays. Three errors present.
=======================================*/

// different variables specified at once
var favoriteShows, text, SLen, i;

// array of 4 different movies/Tv shows
//one error
favoriteShows = ["Mr. Robot", "Dark", "Money Heist", "The Good Doctor";
FavLen = favoriteShows.length;

//the for loop will go through each array.
// one error
text = "";
for (i = 0; i < FavLen; i++)
  text += favoriteShows[i] + '\n';
}

// Output should be the 4 shows specified in favoriteShows in a different line each.
// one error
console.log(text());
